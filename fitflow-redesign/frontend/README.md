React Native app goes here.

This folder will contain the FitFlow mobile frontend (iOS/Android), built with React Native, as recommended in Lab 5, Activity 1.

Planned structure:
- src/screens/       — Home Dashboard, AI Workout Planner, Progress Tracking, Community Feed, Nutrition Logger
- src/components/    — shared UI components (cards, buttons, nav bar)
- src/services/      — API client for the backend
- src/assets/        — images, icons, fonts

Setup (once code is added):
    npm install
    npx react-native run-ios     # or run-android
